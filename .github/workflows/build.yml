name: Build and Release APKname: Build and Release APK

on:
  push:
    branches: [ main, master ]
  workflow_dispatch:

jobs:
  build:
    name: Build APK
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
      
      - name: Setup Java 17
        uses: actions/setup-java@v4
        with:
          distribution: 'temurin'
          java-version: '17'
          cache: 'gradle'
      
      - name: Setup Flutter
        uses: subosito/flutter-action@v2
        with:
          # ترك الإصدار فارغاً سيجعل GitHub يجلب أحدث نسخة مستقرة لعام 2026
          flutter-version: '' 
          channel: 'stable'
          cache: true

      - name: Flutter Doctor
        run: flutter doctor -v

      - name: Clean Project
        run: flutter clean 
      
      - name: Get dependencies
        run: flutter pub get
      
      - name: Run code generation
        run: dart run build_runner build --delete-conflicting-outputs
      
      - name: Build APK (Release)
        run: flutter build apk --release --no-tree-shake-icons
      
      - name: Upload APK Artifact
        uses: actions/upload-artifact@v4
        with:
          name: mulaazim-apk-${{ github.run_number }}
          path: build/app/outputs/flutter-apk/app-release.apk


on:
  push:
    branches: [ main, master ]
  pull_request:
    branches: [ main, master ]
  workflow_dispatch:

jobs:
  build:
    name: Build APK
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
      
      - name: Setup Java 17
        uses: actions/setup-java@v4
        with:
          distribution: 'temurin'
          java-version: '17'
          cache: 'gradle'
      
      - name: Setup Flutter
        uses: subosito/flutter-action@v2
        with:
          flutter-version: '3.27.3' 
          channel: 'stable'
          cache: true

      # تصحيح إصدار الـ SDK في pubspec لتجنب تعارض الـ 3.9.2
      - name: Fix SDK version in pubspec.yaml
        run: |
          sed -i 's/sdk: .*/sdk: ">=3.0.0 <4.0.0"/' pubspec.yaml
      
      - name: Flutter Doctor
        run: flutter doctor -v

      # تم تصحيح الأمر هنا من flutter pub clean إلى flutter clean
      - name: Clean Project
        run: flutter clean 
      
      - name: Get dependencies
        run: flutter pub get
      
      - name: Run code generation
        run: dart run build_runner build --delete-conflicting-outputs
      
      - name: Analyze code
        run: flutter analyze
        continue-on-error: true
      
      - name: Build APK (Release)
        run: flutter build apk --release --no-tree-shake-icons
      
      - name: Upload APK Artifact
        uses: actions/upload-artifact@v4
        with:
          name: mulaazim-apk-${{ github.run_number }}
          path: build/app/outputs/flutter-apk/app-release.apk
          if-no-files-found: error
          retention-days: 30
      
      - name: Get APK Info
        if: success()
        run: |
          echo "APK Size: $(du -h build/app/outputs/flutter-apk/app-release.apk | cut -f1)"
          echo "APK Path: build/app/outputs/flutter-apk/app-release.apk"
